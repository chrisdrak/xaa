# Δημιουργία του APK

Ο φάκελος αυτός είναι έτοιμο project για `flet build apk`. Δεν χρειάζεται
να αλλάξεις τίποτα στον κώδικα — μόνο να τρέξει το build.

```
xa_screener_apk/
├── pyproject.toml              ρυθμίσεις εφαρμογής και build
├── requirements.txt
├── assets/
│   ├── icon.png                εικονίδιο εφαρμογής (1024×1024)
│   └── splash.png              οθόνη εκκίνησης
├── src/
│   ├── main.py                 σημείο εκκίνησης
│   ├── xa_flet.py              το UI
│   └── xa_core.py              λήψη δεδομένων + χαρτοφυλάκιο
└── .github/workflows/build-apk.yml
```

---

## Τρόπος 1 — στο GitHub, χωρίς να εγκαταστήσεις τίποτα (προτεινόμενο)

Χτίζει το APK σε servers της GitHub. Δωρεάν για δημόσια αποθετήρια.
Θέλει μόνο λογαριασμό GitHub.

1. Φτιάξε νέο repository στο github.com (π.χ. `xa-screener`).
2. Ανέβασε **όλο** τον φάκελο — μαζί με τον κρυφό `.github`.
   Αν το κάνεις με drag & drop στη σελίδα του GitHub, ο φάκελος `.github`
   συνήθως δεν ανεβαίνει· τότε φτιάξε το αρχείο χειροκίνητα:
   *Add file → Create new file* και γράψε ως όνομα
   `.github/workflows/build-apk.yml`, επικολλώντας το περιεχόμενο.
3. Πήγαινε στην καρτέλα **Actions** → *Build APK* → **Run workflow**.
4. Περίμενε 10–20 λεπτά (το πρώτο build αργεί).
5. Μόλις τελειώσει, στο κάτω μέρος της σελίδας του build υπάρχει το
   **Artifacts → xa-screener-apk**. Κατέβασέ το — μέσα είναι το APK.

Κάθε φορά που αλλάζεις κάτι στον κώδικα, ξανατρέχεις το workflow και
παίρνεις νέο APK.

## Τρόπος 2 — τοπικά στον υπολογιστή σου

Χρειάζεται εγκατάσταση, μία φορά:

| Τι | Πού |
|---|---|
| Python 3.9+ | python.org |
| Flutter SDK | flutter.dev — και να μπει στο PATH |
| JDK 17 | π.χ. Temurin |
| Android SDK | έρχεται με το Android Studio |

Μετά:

```
pip install "flet[all]"
cd xa_screener_apk
flet build apk
```

Το APK βγαίνει στο `build/apk/`. Το πρώτο build κατεβάζει αρκετά GB και
θέλει χρόνο· τα επόμενα είναι πολύ γρηγορότερα.

---

## Εγκατάσταση στο κινητό

1. Αντέγραψε το `.apk` στο τηλέφωνο (καλώδιο, Drive, ή στείλ' το στον εαυτό σου).
2. Άνοιξέ το από τον διαχειριστή αρχείων.
3. Το Android θα ζητήσει άδεια «εγκατάσταση από άγνωστες πηγές» για την
   εφαρμογή απ' όπου το ανοίγεις — δώσ' την και συνέχισε.

Επειδή το APK δεν είναι υπογεγραμμένο με επίσημο κλειδί, το Play Protect
μπορεί να εμφανίσει προειδοποίηση. Είναι αναμενόμενο για εφαρμογή που
φτιάχνεις μόνος σου· πάτα «Εγκατάσταση ούτως ή άλλως».

---

## Ρυθμίσεις που μπορεί να θέλεις να αλλάξεις

Στο `pyproject.toml`:

- `product` — το όνομα κάτω από το εικονίδιο
- `org` — αντίστροφο domain· μαζί με το όνομα φτιάχνει το bundle id
- `version` και `build_number` — αύξησέ τα σε κάθε νέα έκδοση

Το εικονίδιο αλλάζει αντικαθιστώντας το `assets/icon.png`
(τετράγωνο PNG, ιδανικά 1024×1024).

## Δύο πράγματα να ξέρεις

Η άδεια `INTERNET` είναι ήδη δηλωμένη — χωρίς αυτήν η εφαρμογή δεν θα
μπορούσε να κατεβάσει τιμές.

Το χαρτοφυλάκιο αποθηκεύεται **μέσα στο κινητό**, ξεχωριστά από αυτό του
υπολογιστή. Οι δύο δεν συγχρονίζονται μεταξύ τους.
